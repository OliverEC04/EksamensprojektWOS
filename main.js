///<reference path="p5.global-mode.d.ts" />
const canvasSize =
{
    x: 1000,
    y: 800
};

function setup()
{
    createCanvas(canvasSize.x, canvasSize.y);

}

function draw()
{
    background(0, 0, 0);

}